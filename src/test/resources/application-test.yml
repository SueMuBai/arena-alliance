spring:
  datasource:
    url: jdbc:h2:mem:alliance;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

alliance:
  data-dir: ./target/test-data
  secret: test-secret-0123456789abcdef
  dev-login-enabled: true
